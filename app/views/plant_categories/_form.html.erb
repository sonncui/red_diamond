<%= form_with(model: plant_category, local: true) do |form| %>
  <% if plant_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(plant_category.errors.count, "error") %> prohibited this plant_category from being saved:</h2>

      <ul>
      <% plant_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :plant_category_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
